import Link from"next/link";import{requireSession}from"../lib/auth/session";import{can}from"../lib/auth/permissions";import{navigation}from"../components/shell/navigation";
export default async function HomePage(){const user=await requireSession();const categories=navigation.map(c=>({...c,items:c.items.filter(i=>can(user.roles,i.permission))})).filter(c=>c.items.length);return <main className="min-h-[calc(100dvh-4rem)] bg-zinc-950 p-4 text-white sm:p-7"><div className="mx-auto max-w-7xl space-y-7"><header className="rounded-3xl border border-emerald-400/20 bg-gradient-to-br from-emerald-400/10 to-transparent p-6 sm:p-9"><p className="text-xs font-black tracking-[.35em] text-emerald-400">FIXAR OS</p><h1 className="mt-3 text-3xl font-black sm:text-5xl">Dijital Fabrika Merkezi</h1><p className="mt-3 max-w-2xl text-zinc-400">Üretimden finansa, stoktan bakıma tüm yetkili operasyonlarınıza tek noktadan erişin.</p><p className="mt-5 text-sm text-zinc-300">Hoş geldiniz, <b>{user.userName??user.email}</b></p></header><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{categories.map(c=><article className="group rounded-3xl border border-white/10 bg-zinc-900 p-5 transition hover:-translate-y-0.5 hover:border-emerald-400/40" key={c.id}><div className="flex items-start justify-between gap-3"><div><p className="text-xs font-black uppercase tracking-wider text-emerald-400">{c.items.length} modül</p><h2 className="mt-2 text-xl font-black">{c.label}</h2></div><span className="flex h-11 w-11 items-center justify-center rounded-2xl bg-white/5 text-lg font-black text-emerald-300">{c.label[0]}</span></div><p className="mt-3 min-h-10 text-sm text-zinc-400">{c.description}</p><div className="mt-5 flex flex-wrap gap-2">{c.items.slice(0,4).map(i=><Link className="min-h-11 rounded-xl bg-white/5 px-3 py-3 text-xs font-semibold text-zinc-300 hover:bg-emerald-400 hover:text-black" href={i.href} key={i.href}>{i.label}</Link>)}</div>{c.items.length>4&&<p className="mt-3 text-xs text-zinc-600">+ {c.items.length-4} modül daha menüde</p>}</article>)}</section></div></main>}
