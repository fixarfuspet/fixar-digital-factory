"use client";import{useEffect,useState}from"react";import{FinanceOpsNav}from"../components/finance/FinanceOpsNav";export default function Page(){const[liquidity,setLiquidity]=useState<Record<string,number|string>[]>([]),[daily,setDaily]=useState<Record<string,number|string>[]>([]),[maturity,setMaturity]=useState<Record<string,unknown>[]>([]);useEffect(()=>{Promise.all([fetch("/api/backend/v1/cash-flow/liquidity"),fetch("/api/backend/v1/cash-flow/daily"),fetch("/api/backend/v1/cash-flow/cheque-maturity")]).then(async r=>{const j=await Promise.all(r.map(x=>x.json()));setLiquidity(j[0].data??[]);setDaily(j[1].data??[]);setMaturity(j[2].data??[])})},[]);return <main className="min-h-screen bg-zinc-950 p-6 text-white"><div className="mx-auto max-w-7xl space-y-5"><h1 className="text-3xl font-black">Nakit Akışı ve Likidite</h1><FinanceOpsNav/><section className="grid gap-3 md:grid-cols-4">{liquidity.flatMap(x=>[[`${x.currency} Likit`,x.totalLiquidBalance],[`${x.currency} Portföy Çeki`,x.chequePortfolioAmount],[`${x.currency} 7 Günlük Çek`,x.chequesDueIn7Days],[`${x.currency} Karşılıksız`,x.bouncedChequeAmount]]).map(([k,v])=><div className={box} key={String(k)}><small>{k}</small><p className="text-xl font-black">{Number(v).toLocaleString("tr-TR")}</p></div>)}</section><section className={box}><h2 className="font-black">Günlük Nakit Akışı</h2><pre className="mt-3 overflow-auto text-xs">{JSON.stringify(daily,null,2)}</pre></section><section className={box}><h2 className="font-black">Yaklaşan Çekler</h2><pre className="mt-3 overflow-auto text-xs">{JSON.stringify(maturity,null,2)}</pre></section></div></main>}const box="rounded-2xl border border-white/10 bg-white/5 p-4";
